---
'@quill/shared': minor
---

`admin.submissions.filters`: the copy for filtering and sorting the
submissions from the column headings (the funnel's name, the menus, the chips
above the table, the no-match state and the Summary's bar links), in English
and Spanish. `statusAll`, `statusCompleted` and `statusPartial` are gone with
the status buttons they labelled: the Status column filters now.
