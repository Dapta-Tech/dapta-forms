# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

When you make a change that should be released, run `pnpm changeset` and follow
the prompts. Commit the generated markdown file with your PR — the release
workflow consumes it to version packages and write the changelog.
